# MSBuildTasks

A set of MSBuild tasks / tools to allow consuming the package built by a `<ProjectReference>` in test projects.

This likely could be extracted to it's own package, so creating as a separate project, but adding as part of Verify.Nupkg for now
so users can start to play with the APIs and to make a proposal back to the SDK.